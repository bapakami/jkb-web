# JKB Web — Sesi Kerja

## Objective
- Selesaikan tampilan & fitur situs JKB (`D:\Workspace\jkb_web`): fix 500 `/karir/iso`, perbaiki input "Posisi" admin, dropdown "Tipe Pekerjaan", halaman Traffic Website, tombol "Buka di Google Maps", perbaikan grafik Tren Kunjungan Harian.
- **Saat ini**: memperbaiki filter "Rentang Tanggal" & "Halaman" di `/admin/traffic-web` yang tidak mengubah data (angka ringkas & grafik) — SELESAI & TERVERIFIKASI.

## Important Details
- **Environment**: HDD + Windows Defender exclusion; `artisan serve` di `127.0.0.1:8000`; Filament v3.3.55 + Livewire v3.8.8; MySQL `DB_DATABASE=jkb`; php di `C:\php\php.exe`.
- **Panel admin**: `->topNavigation()`, warna primary Amber, `->login()` default User model, middleware `EnsurePanelAccessToken`; **tidak ada role**.
- **Invoice dibayar penuh (uang muka=total)**: metode `getNetTotal()`/`getNumeral()` sdh menangani nilai 0; seluruh logika Cart/wa-coba net netral (soft patch), tanpa memaksa Pebisnis mengubah perilaku.
- **(COMING) Filter binding di Page Filament**: form page WAJIB `->statePath('data')`. Trait `InteractsWithForms::form()` default menerapkan `->statePath(getFormStatePath())` yang `null`, dan jika method `form()` dioverride penuh tanpa statePath → field menulis ke property root Livewire (`$this->range`), BUKAN `$this->data`, sehingga `$this->data[...]` di query selalu fallback default. `getViewData()` dipanggil ulang tiap render → begitu binding benar, semua angka + chart + tabel ikut berubah real-time.
- **Verifikasi rantai (terbukti lewat Livewire::test via temp PHP)**: `set('data.range','today')` → `data.range='today'`, trend=1 titik; `set('data.url','/')` → total 30d menyusut 28→15; default 7d: 28 total / 3 unik / 7 poin trend.
- **CSS admin**: Filament theme prebuilt (`public/css/filament/filament/app.css`) HANYA memuat kelas tertentu — `.flex-1`, `.justify-end`, `.items-end`, `.gap-1.5`, `.gap-4`, `.sm:grid-cols-3`, `.text-3xl`, `.font-bold`, `.text-gray-950`, `.flex-col`, `.h-full`, `.grid-*` ADA; `h-56`, `max-w-[32px]`, `text-[10px]`, `leading-3` TIDAK ADA → markup chart/page admin baru WAJIB pakai inline styles (bar chart ditulis ulang dengan inline style; Selesai).
- **Career "Tipe Pekerjaan"**: Select key `Full-time`/`Part-time`/`Magang` (label "Full time"/"Part time"); `->required()`; filter tabel 3 opsi (Kontrak dihapus); data outlier `'FULL TIME '` (id 4 ISO) dinormalisasi.
- **Autoslug**: 7 resource (Career, Product, ProductCategory, News, Project, Branch, TahukahAnda) judul/nama pakai `->lazy()` (= blur), bukan `->live()` — cegah morph revert saat typing di HDD; slug ter-fill saat blur. Field filter pada halaman TrafficWeb pakai `->live()` (aman).
- **Blade**: komentar `{{-- --}}` tidak boleh di dalam `@php`.
- **Pola tes PHP di Windows/PS**: gunakan file temp `.php` (di `C:\Users\JKB\AppData\Local\Temp\opencode\`) + `require 'D:/Workspace/jkb_web/vendor/autoload.php'` + bootstrap, atau `Livewire::test(Class::class)` untuk simulasi update filter end-to-end; hindari `php -r` dengan escaping bertingkat.
- **Klik peta cabang**: hanya `resources/views/home.blade.php`; popup "Buka di Google Maps" = `https://www.google.com/maps?q=LAT,LNG` `target="_blank"`; card `<a>`→`<div role="button" data-map-index>` + guard JS; `--jkb-item:100px`; 6 koordinat cabang di DB + fallback seeder.

## Work State
### Completed
- **Fix 500 `/karir/iso`**: accessor `getRequirementsListAttribute()` (`app/Models/Career.php`, normalisasi array/objek `{syarat}`→array string); `careers/show.blade.php` pakai `$career->requirements_list`. Semua halaman karir 200.
- **Autoslug**: selesai di 7 resource (`->lazy()`), runtime `['blur']`, lint bersih.
- **Tipe Pekerjaan → Select+filter+pembersihan data**.
- **TrafficWeb**: migration `create_page_visits_table`, `PageVisit` model, middleware `RecordPageVisit` (GET 200, skip `/admin*`,`/up`, JSON/ajax, bot, UA kosong) di `bootstrap/app.php`; page `TrafficWeb` (grup Analitik, filter range today/7/30/kustom + Select URL, stats total/unik-IP/top-5, tabel sortable `visited_at desc`, empty-state); `$view` static; 2+ kunjungan tercatat; route `admin/traffic-web`.
- **Filter binding data (FIXEK TERBARU)** `app/Filament/Pages/TrafficWeb.php`: tambah `->statePath('data')` → `data.range`/`data.url`/`data.from`/`data.to`; `dateRange()`/`baseQuery()` baca `$this->data[...]`; `getViewData()` terpanggil tiap render (live-on) → semua elemen update real-time. Verifikasi Livewire::test PASS. Bonus off-by-one: range numerik kini `subDays($range-1)` → "7 hari" = 7 titik.
- **Chart Tren** (`resources/views/filament/pages/traffic-web.blade.php`): root cause = utility Tailwind arbitrary tak ada di tema admin (`h-56` → container collapse, bar 0px); ditulis ulang murni inline styles (container `height:13rem`, bar kuning brand %height, `min-height:3px` untuk hari 0, label 10px). Terverifikasi compile.
- **Google Maps** (`home.blade.php`): tombol popup, guard click, keyboard support, ukuran card 100px.
- (riwayat): glassmorphism, fix navbar `{`, scroll Sebaran Cabang, kalkulator beton.

### Active
- (none)

### Blocked
- (none)

## Next Move
1. Minta user hard-refresh `/admin/traffic-web` (Ctrl+F5) → cek Total/Pengunjung Unik/Terpopuler/grafik berubah saat pilih "Hari ini" vs "7 hari terakhir" vs pilih halaman tertentu.
2. (Opsional/F12) Cek tab Network: payload Livewire kini memuat `data.range`/`data.url`, bukan `range`/`url` di root.
3. Data kunjungan masih sedikit (semua ~28 masuk hari ini); untuk menguji beda rentang lebih kentara, bisa generate kunjungan tanggal lama (mis. 2-5 hari lalu) via IWR + SQL `UPDATE page_visits SET visited_at = ...`.

## Relevant Files
- `app/Filament/Pages/TrafficWeb.php`: filter form + `->statePath('data')` (perbaikan inti) + dateRange/baseQuery/trendData.
- `resources/views/filament/pages/traffic-web.blade.php`: chart inline-style.
- `public/css/filament/filament/app.css`: daftar utility yang ada/tidak.
- `app/Http/Middleware/RecordPageVisit.php`, `bootstrap/app.php`, `app/Models/PageVisit.php`, `database/migrations/2026_09_11_000000_create_page_visits_table.php`.
- `app/Models/Career.php` (`requirements_list`), `resources/views/pages/careers/show.blade.php`.
- `app/Filament/Resources/CareerResource.php` (+ 6 resource lain, `->lazy()`); `resources/views/home.blade.php`, `resources/css/app.css`, `resources/views/components/concrete-calculator.blade.php`, `resources/views/partials/navbar.blade.php`.